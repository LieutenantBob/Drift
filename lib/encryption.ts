// Stub — AES-256-GCM encryption, implement in Session 2
export function encrypt(plaintext: string): string {
  return plaintext;
}

export function decrypt(ciphertext: string): string {
  return ciphertext;
}
